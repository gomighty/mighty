{
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@types/bun": "^1.2.23",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "ci:biome": "biome ci",
    "ci:typecheck": "bun run --filter '*' typecheck",
    "build-pkg": "bun run --filter '*' build-pkg"
  }
}
