{
  "type": "module",
  "packageManager": "pnpm@10.30.1",
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@types/node": "^25.3.5",
    "tsdown": "^0.21.0",
    "typescript": "^5.9.2",
    "vitest": "^3.1.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  },
  "scripts": {
    "ci:biome": "biome ci",
    "ci:phpstan": "cd packages/laravel && vendor/bin/phpstan analyse --memory-limit=512M",
    "build": "tsdown",
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run"
  }
}
