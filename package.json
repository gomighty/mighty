{
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@types/bun": "^1.2.19",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "ci:biome": "biome ci",
    "ci:vitest": "bun run --filter '*' vitest --reporter=github-actions",
    "vitest": "bun run --filter '*' vitest --reporter=verbose"
  }
}
