{
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@types/bun": "^1.2.19",
    "@typescript/native-preview": "^7.0.0-dev.20250903.1",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "ci:biome": "biome ci",
    "build-pkg": "bun run --filter '*' --elide-lines=0 build-pkg",
    "ci:vitest": "bun run --filter '*' --elide-lines=0 vitest --reporter=github-actions",
    "vitest": "bun run --filter '*' vitest --reporter=verbose"
  }
}
